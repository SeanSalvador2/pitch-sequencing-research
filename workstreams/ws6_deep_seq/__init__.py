"""Workstream 6 -- deep sequence models: GRU (+ optional compact Transformer demo).

Does a *learned* representation of the ordered plate-appearance sequence beat the
engineered tabular history (WS3) and the explicit variable-order grammar (WS2)?

See :mod:`workstreams.ws6_deep_seq.model` for the capacity-matched view architectures and
:mod:`workstreams.ws6_deep_seq.run_ws6` for the runnable pipeline. PyTorch lives in the
``[deep]`` optional-dependency group: ``pip install -e ".[deep]"``.
"""
